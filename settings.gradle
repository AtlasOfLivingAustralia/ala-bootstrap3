rootProject.name='ala-bootstrap3'

// Inplace plugin config
// Set to true it needed only for development
boolean inplace = true

if (inplace) {
    include ':ala-auth'
    project(':ala-auth').projectDir = new File(settingsDir,'../ala-auth-plugin')
}


